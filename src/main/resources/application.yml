spring:
  application:
    name: power-stock
  datasource:
    url: ${DATABASE_URL:jdbc:postgresql://aws-1-ap-northeast-1.pooler.supabase.com:5432/postgres}
    username: ${DATABASE_USERNAME:postgres.qhcwyirmzumtmxqgffhs}
    password: ${DATABASE_PASSWORD:NAAHnkKm3C78GqzM}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect
  flyway:
    baseline-on-migrate: true
    baseline-version: 0
    enabled: true
    locations: classpath:db/migration
  data:
    redis:
      host: ${REDIS_HOST:safe-cardinal-110845.upstash.io}
      port: ${REDIS_PORT:6379}
      password: ${REDIS_PASSWORD:gQAAAAAAAbD9AAIgcDFkMmExZjFhMGZjNDY0MzVkODcxZGFhMmM1ZWNlZmY5Ng}

jwt:
  secret: ${JWT_SECRET:defaultSecretKeyThatMustBeAtLeast256BitsLongForHS256Algo}
  expiration: 86400000
  refresh-expiration: 604800000

server:
  port: 8080
---
spring:
  config:
    activate:
      on-profile: test
  datasource:
    url: jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect
  flyway:
    enabled: false
  data:
    redis:
      host: localhost
      port: 6379